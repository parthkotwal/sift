# Yelp Open Dataset — license prohibits redistribution. Never commit data.
data/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Notebooks / scratch
.ipynb_checkpoints/
scratch/

# Terraform — local state is retained until teardown succeeds, never committed.
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
terraform.tfvars
*.auto.tfvars

# OS / editor
.DS_Store

# Cross-review output (scripts/cross-review.sh) — scratch notes, promote real
# findings into .agents/ISSUES.md by hand.
.agents/reviews/
